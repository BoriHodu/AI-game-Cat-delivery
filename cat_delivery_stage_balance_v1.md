# 고양이 택배정리 게임 — 스테이지 1~20 밸런스 설계 (v1 초안)

> 이 문서는 `index.html`의 실제 데이터 구조(CATEGORIES, ALL_ITEMS, CHARS, SV_PHASES,
> BOX_SLOT_POSITIONS 등)를 확인한 뒤 작성한 스토리 모드(20스테이지) 밸런스 초안입니다.
> 현재 코드에는 스토리 모드 실제 플레이 로직이 아직 없고(`"스테이지 배송 게임은 준비 중이에요!"`),
> 이 문서가 그 구현의 설계 기준이 됩니다.

---

## 0. 핵심 설계 원칙 (먼저 읽어주세요)

### 클리어 조건: "아이템 개수 채우기" 방식으로
- **실패 조건**: 하트(목숨) 3개 소진 (기존 서바이벌 모드와 동일한 하트 시스템 재사용, 판다씨 캐릭터의 "하트+1" 능력도 그대로 유효)
- **성공 조건**: 스테이지별 목표 개수만큼 정답 분류
- **시간은 "실패 조건"이 아니라 "점수 보너스 조건"으로만 사용** (수정) — 별점(★) 같은 새 UI/저장 시스템을 새로 만들지 않고, 목표 시간 안에 끝내면 그 스테이지 획득 점수에 +30% 보너스만 붙는 방식. 목표 시간을 넘겨도 클리어는 그대로 인정되고, 그냥 보너스만 없음. 새 UI 없이 기존 점수판 하나로 끝나서 구현 부담이 적음. (구체적인 스테이지별 목표 시간은 3번 섹션 참고)
- 시간 압박(진짜 버티기)은 이미 서바이벌 모드의 정체성이므로, 스토리 모드는 시간제한 없이 "차분히 완료하는" 느낌으로 차별화.

### 콤보 시스템: 서바이벌 모드 것을 그대로 재사용
- 서바이벌 모드에 이미 있는 `COMBO_TIERS`(3콤보=×1.2, 6콤보=×1.5, 10콤보=×2) + `getComboMultiplier()`를 스토리 모드에도 그대로 적용
- 정답 분류 성공 → 콤보 +1, 획득 점수에 현재 콤보 배율 곱해서 지급
- 오답 분류 → 콤보 0으로 초기화 + 하트 1개 감소 (서바이벌 모드와 동일한 동작)
- 새로 설계할 부분 없이 기존 로직을 스토리 모드 루프에 연결만 하면 됨 → 구현 난이도 낮음

### 아이템 목표 개수: "최소 1분 플레이타임" 기준으로 역산 (수정)
- 예전 공식(목표개수=10+스테이지)으로는 사실 스테이지1뿐 아니라 **20개 전부 1분 미만**이었음 (21~33초 수준) → 전면 재설계
- 챕터별 목표 시간(2번 섹션 spawnBase 기준 역산): 1챕터 60초 · 2챕터 65초 · 3챕터 70초 · 4챕터 75초 (최종보스 스테이지20만 85초)
- 이 시간에 맞춰 목표 개수를 역산했더니, 후반 챕터로 갈수록 스폰 속도가 빨라져서 같은 시간 안에 훨씬 많은 개수가 자연스럽게 나옴 (예: 1스테이지 31개 vs 20스테이지 90개) — 개수 자체가 "얼마나 빠르고 바쁜 챕터인지"를 보여주는 지표가 됨
- 구체적인 스테이지별 목표 개수는 아래 1번 섹션 표 / 정확한 계산식은 3번 섹션 참고

### 박스 개수: 챕터당 +1 (2→3→4→5)
- 골목마을(1~5) = 2개, 해안마을(6~10) = 3개, 숲속마을(11~15) = 4개, 눈꽃마을(16~20) = 5개
- 5개가 현재 UI가 지원하는 물리적 최대치와 정확히 일치 → 새 UI 작업 불필요

### 보스 스테이지: 5 / 10 / 15 / 20
- 캐릭터 해금 시점과 정확히 겹침 → "챕터 클리어 = 보스전 + 새 캐릭터 해금"으로 자연스럽게 연결
- 보스 기믹은 새 시스템을 만들기보다 **기존 코드에 있는 값을 일시적으로 극단화**하는 방식 추천 (아래 4번 참고)

---

## 1. 스테이지별 컨셉 / 박스 카테고리

박스 카테고리는 챕터 테마에 어울리게 배치했고, 새 대분류(가전/주방/문구/욕실/식물)는
2챕터부터 하나씩 자연스럽게 소개되도록 배치했습니다. **완벽히 일치할 필요 없다고 하셨으니,
이름/컨셉은 얼마든지 바꾸셔도 됩니다 — 카테고리 조합과 목표 개수만 밸런스 기준으로 참고해주세요.**

### 챕터 1: 골목마을 (박스 2개)
| 스테이지 | NPC / 컨셉 | 박스 카테고리 | 목표 개수 |
|---|---|---|---|
| 1 | 미미의 첫 주문 | 장난감, 간식 | 31 |
| 2 | 베이커리의 모카 | 간식, 책 | 31 |
| 3 | 편집샵 루루 | 장난감, 옷 | 32 |
| 4 | 문구점 아저씨 사부 | 책, 쓰레기(분리수거) | 33 |
| 5 **(보스)** | 헌옷가게 코코 — 골목마을 대청소 | 옷, 쓰레기 | 34 |

### 챕터 2: 해안마을 (박스 3개) — 신규 카테고리 "욕실용품", "가전" 등장
| 스테이지 | NPC / 컨셉 | 박스 카테고리 | 목표 개수 |
|---|---|---|---|
| 6 | 서프샵 나나 | 장난감, 옷, 욕실용품 | 38 |
| 7 | 아이스크림가게 보리 | 간식, 장난감, 쓰레기 | 39 |
| 8 | 등대지기 하루 | 책, 가전, 욕실용품 | 40 |
| 9 | 조개공방 유자 | 옷, 문구용품, 쓰레기 | 42 |
| 10 **(보스)** | 요트클럽 대장 모카투 — 해안마을 큰 파티 | 장난감, 간식, 가전 | 43 |

### 챕터 3: 숲속마을 (박스 4개) — 신규 카테고리 "식물", "주방용품" 등장
| 스테이지 | NPC / 컨셉 | 박스 카테고리 | 목표 개수 |
|---|---|---|---|
| 11 | 원예상점 로즈 | 장난감, 책, 식물, 간식 | 49 |
| 12 | 오두막 셰프 두부 | 간식, 주방용품, 옷, 쓰레기 | 51 |
| 13 | 캠핑샵 조이 | 장난감, 옷, 식물, 가전 | 53 |
| 14 | 버섯마을 약초상 | 책, 식물, 주방용품, 욕실용품 | 55 |
| 15 **(보스)** | 숲의 수호자 — 숲속마을 축제 | 장난감, 간식, 옷, 책 | 58 |

### 챕터 4: 눈꽃마을 (박스 5개) — 마지막 신규 카테고리 "문구용품" 재등장, 전 카테고리 총출동
| 스테이지 | NPC / 컨셉 | 박스 카테고리 | 목표 개수 |
|---|---|---|---|
| 16 | 눈사람공방 하양이 | 장난감, 간식, 옷, 책, 쓰레기 | 65 |
| 17 | 온천여관 유키 | 욕실용품, 주방용품, 옷, 간식, 식물 | 68 |
| 18 | 스케이트샵 은비 | 장난감, 가전, 문구용품, 옷, 쓰레기 | 72 |
| 19 | 크리스마스마켓 코코넛 | 간식, 식물, 책, 가전, 주방용품 | 75 |
| 20 **(최종 보스)** | 대표 미미 — 눈꽃마을 그랜드 오픈 | 장난감, 간식, 옷, 책, 쓰레기 (전 카테고리 총집합) | 90 |

---

## 2. 난이도 곡선 (기술적 튜닝 값)

기존 `SV_PHASES`(서바이벌 모드) 값의 초반 구간을 기준으로, **서바이벌보다 더 완만하게** 잡았습니다.
스토리 모드는 "누구나 편하게 즐기는 코스", 서바이벌은 "진짜 도전"이라는 역할 분담입니다.

| 챕터 | spawnBase (스폰 간격) | trickyRatio (헷갈리는 아이템 비율) | travelMs (아이템 이동시간=반응시간) | 박스교체 | 러시타임 |
|---|---|---|---|---|---|
| 1 (골목마을) | 1700 → 1550ms | 0% → 8% | 6000 → 5600ms | 없음 | 없음 |
| 2 (해안마을) | 1500 → 1300ms | 10% → 18% | 5400 → 5000ms | 스테이지7부터, 1회/스테이지 | 없음 |
| 3 (숲속마을) | 1250 → 1050ms | 20% → 28% | 4800 → 4400ms | 2회/스테이지 | 스테이지13부터, 1회/스테이지 |
| 4 (눈꽃마을) | 1000 → 820ms | 28% → 36% | 4200 → 3800ms | 2~3회/스테이지 | 1~2회/스테이지 |

- 각 챕터 안에서는 스테이지가 진행될수록 표의 왼쪽 값→오른쪽 값으로 선형 보간
- 하트(실수 허용)는 전 스테이지 공통 3개로 고정 (판다씨 캐릭터 능력 "+1"이 항상 의미 있게 유지되도록)

---

## 3. 점수 보너스 (시간 목표) — 스테이지별 목표 시간

**설계 순서가 바뀌었습니다**: 이전엔 "개수 → 시간"을 역산했지만, 이번엔 **"최소 1분" 시간 기준을 먼저 정하고 → 그 시간에 맞는 개수를 역산**했습니다.

- 챕터1(골목마을): 목표 시간 **60초** 고정
- 챕터2(해안마을): 목표 시간 **65초** 고정
- 챕터3(숲속마을): 목표 시간 **70초** 고정
- 챕터4(눈꽃마을): 목표 시간 **75초** 고정, 단 최종보스 스테이지20만 **85초**(그랜드 피날레라 살짝 더 길게)

공식: `목표 개수 = 목표 시간(초) × 1000 ÷ (그 스테이지 spawnBase(ms) × 1.15)`
(spawnBase는 2번 섹션 표에서 챕터 내 선형보간한 스테이지별 정확한 값 사용. 1.15는 반응 여유 버퍼.)

이 시간 안에 목표 개수를 다 채우면 그 스테이지 점수 **+30% 보너스**. 못 채워도 목표 개수만 채우면 클리어는 그대로 인정.

| 스테이지 | 목표 개수 | 목표 시간(초) | | 스테이지 | 목표 개수 | 목표 시간(초) |
|---|---|---|---|---|---|---|
| 1 | 31 | 60 | | 11 | 49 | 70 |
| 2 | 31 | 60 | | 12 | 51 | 70 |
| 3 | 32 | 60 | | 13 | 53 | 70 |
| 4 | 33 | 60 | | 14 | 55 | 70 |
| 5 (보스) | 34 | 60 | | 15 (보스) | 58 | 70 |
| 6 | 38 | 65 | | 16 | 65 | 75 |
| 7 | 39 | 65 | | 17 | 68 | 75 |
| 8 | 40 | 65 | | 18 | 72 | 75 |
| 9 | 42 | 65 | | 19 | 75 | 75 |
| 10 (보스) | 43 | 65 | | 20 (최종보스) | 90 | 85 |

- 모든 스테이지가 최소 60초 이상 확보됨 (요청하신 부분 반영)
- 후반 챕터는 스폰 속도가 훨씬 빨라서, 같은 60~85초 범위 안에서도 목표 개수가 크게 늘어남 (1스테이지 31개 → 20스테이지 90개) — 자연스럽게 "후반부는 훨씬 바쁘고 몰아친다"는 체감 난이도로 이어짐
- 콤보 보너스와 시간 보너스는 별개로 동시 적용 (콤보를 잘 유지하면서 시간 안에 끝내면 두 배로 이득)

---

## 4. 보스 스테이지 기믹 (5 / 10 / 15 / 20)

새 시스템을 새로 만들기보다, **이미 있는 값(travelMs, runBoxSwap)을 순간적으로 극단화**해서
개발 부담 없이 임팩트를 내는 방향으로 짰습니다.

| 스테이지 | 기믹 이름 | 내용 | 구현 난이도 |
|---|---|---|---|
| 5 | 더블 러시 | 러시타임을 연속 2번 붙여서 발동 (기존 러시 로직 재사용, 간격만 0으로) | 낮음 |
| 10 | 동시 교체 | 박스 3개가 동시에 한꺼번에 바뀜 (기존 `runBoxSwap`을 배치로 한번에 호출) | 낮음 |
| 15 | 택배 폭주 타임 | 스테이지 중반 10초간 `travelMs`를 절반으로 줄여서 컨베이어가 확 빨라짐 | 낮음 |
| 20 | 그랜드 오픈 (종합) | 러시 2회 + 폭주타임 10초 + 박스 동시교체를 한 스테이지 안에 모두 포함 | 중간 (위 3개 조합이라 개별 구현은 이미 끝난 상태) |

---

## 5. 클로드 코드에게 그대로 전달할 수 있는 요청 예시

```
cat_delivery_stage_balance_v1.md 파일 내용 기준으로 스토리 모드(20스테이지) 
게임 로직을 구현해줘. 
- STAGE_INFO를 이 표 내용으로 교체
- 스테이지별 박스 개수/카테고리/목표 개수를 데이터로 분리해서 관리
- 클리어 조건은 목표 개수 달성, 실패 조건은 하트 3개 소진
- 난이도 곡선(spawnBase/trickyRatio/travelMs)은 2번 표 기준으로 챕터별 보간
- 콤보 시스템은 기존 서바이벌 모드의 COMBO_TIERS/getComboMultiplier를 
  그대로 재사용해서 스토리 모드에도 적용해줘
- 3번 표의 목표 시간 안에 클리어하면 그 스테이지 점수에 +30% 보너스를 줘
- 보스 스테이지(5/10/15/20) 기믹은 4번 표 순서대로 구현
- 기존 서바이벌 모드(startSurvival, svLoop) 코드는 최대한 재사용하고, 
  스토리 전용 함수는 별도로 분리해줘
```

---

## 6. 다음에 다듬을 만한 것들 (1차 밸런스 이후)

- 실제 플레이테스트 후 목표 개수/트리키 비율 미세 조정
- **후반 챕터 목표 개수가 꽤 많아졌음(4챕터 65~90개)** — 실제 플레이해보고 하트 3개 고정이 너무 빡빡하게 느껴지면, 후반 챕터만 하트 4개로 늘리는 것도 고려해볼 것
- 시간 보너스 +30%가 체감상 너무 크거나 작으면 비율 조정 (콤보 배율까지 겹치면 생각보다 커질 수 있어서 실제 플레이 후 확인 필요)
- 챕터별 신규 카테고리를 코코넛/미미 같은 이름 대신 실제 스토리 설정에 맞게 교체
- 필요시 대분류 카테고리 자체를 추가 (계획하고 계신 부분)
